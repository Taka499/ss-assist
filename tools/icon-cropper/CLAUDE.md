# Must follow principal

1. Always use `uv run` to execute Python commands.
2. During the test of the program (`uv run python ...` to test), you should wait for my feedback and should NOT kill the running shell.
3. 